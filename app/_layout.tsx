import { Stack } from "expo-router";

/**
 * Renders the root navigation stack for the application.
 *
 * @returns The application's main navigation stack component.
 */
export default function RootLayout() {
  return <Stack />;
}
